pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	
	enviroment {
		$NOTIFICATION_EMAIL = 'test@test.test'
		}
    stages {
        stage('build') {
            steps {
                echo "$NOTIFICATION_EMAIL"
            }
        }
    }
}
